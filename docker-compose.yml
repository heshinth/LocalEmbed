services:
  localembed:
    image: heshinth/localembed:latest
    container_name: localembed
    ports:
      - "8000:8000"
    environment:
      - DEFAULT_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
      # - HF_TOKEN=your_token_here
      # - EMBEDDING_THREADS=8
      # - BATCH_SIZE=256
